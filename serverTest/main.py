from funcoes import processar_dado, enviar_dados_ao_servidor
from interface import sendData


#resultado = sendData()

#while resultado != "sair": 
    
    #print("numero saindao aqui" + str(resultado))
    # processar_dado(resultado)
enviar_dados_ao_servidor("teste")
    # resultado = sendData()


